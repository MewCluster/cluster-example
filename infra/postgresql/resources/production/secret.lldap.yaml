# 实际使用时通过 SOPS 加密此文件
apiVersion: v1
kind: Secret
metadata:
  name: pg-lldap-credentials
  namespace: postgresql
  # 真实部署时，通过 reflector 将此 Secret 同步到应用命名空间
  # annotations:
  #   reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
  #   reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: lldap-authelia
  #   reflector.v1.k8s.emberstack.com/reflection-auto-enabled: "true"
  #   reflector.v1.k8s.emberstack.com/reflection-auto-namespaces: lldap-authelia
stringData:
  password: "changeme-production-password"
